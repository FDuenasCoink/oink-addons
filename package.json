{
  "name": "oink-addons",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "gypfile": true,
  "scripts": {
    "install": "node-gyp-build",
    "rebuild": "node-gyp rebuild",
    "prebuildify-cross": "prebuildify-cross -i linux-arm64 --napi --strip --verbose",
    "prebuild": "prebuildify --napi --strip --electron-compat",
    "prepublishOnly": "prebuildify --napi --strip --electron-compat",
    "clean": "node-gyp clean",
    "compile": "tsc"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.5.9",
    "node-gyp": "^9.4.0",
    "prebuildify": "^5.0.1",
    "prebuildify-cross": "^5.0.0",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "node-addon-api": "^7.0.0",
    "node-gyp-build": "^4.6.1"
  }
}